name: run tests
description: runs the test script of the component
inputs:
- {name: Repo dir, type: Directory}
- {name: component path, type: String}
outputs:
- {name: passed, type: bool}
implementation:
  container:
    image: python:3.7
    command:
    - sh run....
    - inputValue: Branch
    - inputValue: Repo URI
    - outputPath: Repo dir